package main

import "fmt"

// method set is the set of methods for a given type
// a non-pointer recevier works with values that are either pointers or non-pointers
// a pointer receiver works with values that are pointers only

// SEE HIS GO CODE EXAMPLES ON THE PLAYGROUND


func main(){
	fmt.Println(2)
}